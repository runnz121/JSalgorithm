function solution(numbers, hand) {
  var answer = "";
  let idxL;
  let idxR;
  let ch = Array.from({length:12}, ()=>0);

   function DFS(){

   }
   DFS(0,0);
  return answer;
}
let numbers = [1, 3, 4, 5, 8, 2, 1, 4, 5, 9, 5];
let hand = "right";
console.log(solution(numbers,hand)); //"LRLLLRLLRRL"